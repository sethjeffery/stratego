version: 1
display_name: Playwright Visual Test
short_description: Run local browser-based visual checks for Stratego Pulse Arena, especially the SVG board and perspective renderer.
default_prompt: Visually inspect Stratego Pulse Arena with the Playwright MCP server using the local preview server and the ?debugBoard=1 route when board rendering is involved. Fall back to local Playwright scripts only if MCP is unavailable.
